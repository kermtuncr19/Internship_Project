<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

<nav class="navbar navbar-expand-lg border-bottom" style="background-color:#ffea00; border:none;" data-bs-theme="light">
  <div class="container-fluid">
    <!-- LOGO -->
    <a class="navbar-brand d-flex align-items-center" asp-controller="Home" asp-action="Index">
      <img src="~/images/fenerium-logo.png" asp-append-version="true" alt="Fenerium"
           class="d-inline-block align-text-top" style="height:28px;width:auto;object-fit:contain;" />
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- SOL MENÜ -->
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" asp-controller="Product" asp-action="Index">
            <i class="fa-solid fa-list"></i> Ürünler
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" asp-controller="Category" asp-action="Index">
            <i class="fa-solid fa-list"></i> Kategoriler
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" asp-page="/Contact">
            <i class="fa-solid fa-address-card"></i> İletişim
          </a>
        </li>
      </ul>

      <!-- 🔎 NAVBAR SEARCH (ORTA) -->
      <form class="ms-lg-4 me-lg-4 my-2 my-lg-0" method="get"
            asp-controller="Product" asp-action="Index" role="search">
        <div class="input-group nav-search">
          <span class="input-group-text border-end-0"><i class="fa fa-search"></i></span>
          <input type="search"
                 class="form-control border-start-0"
                 name="SearchTerm"
                 value="@(Context.Request.Query["SearchTerm"])"
                 placeholder="Fenerium’da Ara"
                 aria-label="Ara" />
          <button class="btn btn-navy d-none d-md-inline-flex" type="submit">Ara</button>

          @* aktif kategori filtresini korumak istersen *@
          @if (int.TryParse(Context.Request.Query["CategoryId"], out var cid))
          {
              <input type="hidden" name="CategoryId" value="@cid" />
          }
        </div>
      </form>

      <!-- SAĞ MENÜ -->
      <ul class="navbar-nav ms-auto align-items-center gap-2">
        <li class="nav-item cart-slot">
          @await Component.InvokeAsync("CartSummary")
        </li>
        <li class="nav-item">
          <a class="nav-link admin-pill" aria-current="page" asp-area="Admin" asp-controller="Dashboard" asp-action="Index">
            <i class="fa-solid fa-lock"></i> Admin
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<style>

</style>
